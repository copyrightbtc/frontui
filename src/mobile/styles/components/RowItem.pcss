/* stylelint-disable */
.cr-mobile-table-row {
  &__amount {
    display: flex;
    font-size: 14px;
    margin-bottom: 12px;

    &-value {
      padding-right: 5px;
      color: var(--color-dark);
    }

    &-currency {
      text-transform: uppercase;
      color: var(--color-dark);
    }
  }

  &__date {
    font-size: 14px;
  }
}
