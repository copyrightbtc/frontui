.trade-orders-mobile{
  height: 100%;
  &__header{
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: var(--color-dark);
    height: 40px;
    padding: 0 16px;
    border-bottom: 1px solid var(--color-table-lines-grey);
  }
}